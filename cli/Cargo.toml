[package]
name = "launchctl"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "LaunchPad CLI - launchctl command for TontooOS"

[[bin]]
name = "launchctl"
path = "src/main.rs"

[dependencies]
launchpad-lib = { path = "../lib" }
clap = { version = "4", features = ["derive"] }
serde_json = "1.0"
